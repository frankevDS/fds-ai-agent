# FDS AI Agent

Multi-tenant AI business operating platform — Batch 0 (foundation).

This batch contains **only** tenancy, auth, roles/permissions, and
audit logging. No AI, no business data, no integrations yet — see
`docs/ARCHITECTURE.md` for what's deliberately out of scope, and the
full architecture plan document for the roadmap.

Quick start: see `docs/SETUP.md`.

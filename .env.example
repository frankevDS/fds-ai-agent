# --- Server ---
PORT=4000
NODE_ENV=development

# --- Database ---
DATABASE_URL=postgres://fds_app_user:changeme@localhost:5432/fds_ai_agent

# --- Auth ---
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=8h

# --- Reserved for later batches (NOT used in Batch 0) ---
GROQ_API_KEY=
